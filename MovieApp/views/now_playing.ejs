<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="./stylesheets/darkly.css">
    <link rel="stylesheet" type="text/css" media="screen" href="./stylesheets/style.css" />
</head>
<body>
    <div class="wrapper">
        <div class="row">
            <div class="col-sm-12">
                <!-- a really cool trick is you can include another ejs file(html in so many words) with the include command -->
                <%include search-movies%>
                <!-- so ejs has access to res.locals, so there is no need to specify that -->
                <h1>Your IP is <%=ip%></h1>
                <div class="row">
                <%parsedData.forEach((movie)=>{%>
                    <div class="col-sm-3">
                        <img src="<%= imageBaseUrl+movie.poster_path%>"/>
                        <p><%= movie.title %></p>
                        <p><%= movie.overview %></p>
                    </div>
               <% })%>
                </div>
            </div>
        </div>
    </div>
</body>
<!-- <script src="main.js"></script> -->
</html>